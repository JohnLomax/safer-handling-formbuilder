; Overrides the stock php image pool settings for this app.
[global]
daemonize = no

[www]
; Keep Coolify / Docker env vars visible to PHP (forms use getenv()).
clear_env = no
listen = 127.0.0.1:9000
